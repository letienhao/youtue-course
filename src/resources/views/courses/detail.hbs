<div class="mt-4">
    <div class="row">

        <div class="col-lg-3">
            <button>Học ngay</button>
        </div>
        <div class="col-lg-9">
            <h2> {{course.name}}</h2>
            <iframe width="560px" height="315px" src="http://www.youtube.com/watch?v={{course.videoId}}" frameboder="0">
            </iframe>
            <h4>
                {{course.description}}
            </h4>
        </div>
    </div>
</div>